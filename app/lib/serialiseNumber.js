//Convert object values to plain number

// export const serializeDecimal = (obj) => {
//   const serialized = { ...obj };
//   if (obj.balance) {
//     serialized.balance = obj.balance.toNumber();
//   }
//   if (obj.amount) {
//     serialized.amount = obj.amount.toNumber();
//   }
//   if (obj.openingBalance) {
//     serialized.openingBalance = obj.openingBalance.toNumber();
//   }

//   return serialized;
// };
